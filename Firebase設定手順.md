# Firebase 共有データベース設定手順（1回だけ・約15分）

これをやると「誰が・どの店舗で・何を・いくつ」発注したかが
**全スタッフ・全端末でリアルタイム共有**されます。
URLは今のまま（https://ruiysrok.github.io/sonae/）。スタッフはURLを開くだけです。

---

## ステップ1：Firebaseプロジェクトを作る
1. https://console.firebase.google.com/ を開く（Googleアカウントでログイン）
2. **「プロジェクトを作成」** をクリック
3. プロジェクト名：`sonae`（何でもOK）→ 続行
4. Googleアナリティクス：**無効でOK**（オフにして続行）
5. 「プロジェクトを作成」→ 完了まで待つ

## ステップ2：Firestore（データベース）を作る
1. 左メニュー **「構築」→「Firestore Database」**
2. **「データベースを作成」**
3. ロケーション：**asia-northeast1（東京）** を選択
4. **「テストモードで開始」** を選択 → 作成
   - ※テストモードは30日で書き込み不可になります。恒久設定は下の「ステップ5」で。

## ステップ3：Webアプリを登録して設定をもらう
1. プロジェクトの概要（歯車⚙の隣のトップ）→ **「</>」（ウェブ）** アイコンをクリック
2. アプリのニックネーム：`sonae-web` → **「アプリを登録」**
   （Firebase Hostingのチェックは不要）
3. 画面に出る **`const firebaseConfig = { ... }`** のコード（apiKey〜appIdまで）を**コピー**

## ステップ4：設定を私（Claude）に渡す
コピーした firebaseConfig を**チャットに貼ってください**。
- ⚠️ これは「公開してよい接続情報」で、パスワードやトークンとは違い秘密ではありません
  （Webアプリに埋め込む前提のものです）。貼ってOKです。
- 受け取ったら私がアプリに組み込み、デプロイします。以降、発注履歴が全端末で共有されます。

## ステップ5：セキュリティルール（30日以内にやる）
テストモード期限が切れる前に、Firestoreの「ルール」タブで以下に置き換え：

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /orders/{orderId} {
      allow read: if true;
      allow create: if true;
      allow update, delete: if false;  // 記録の書き換え・削除は不可
    }
  }
}
```

- 発注記録の「追加と閲覧」だけ許可し、改ざん・削除を防ぐ設定です。
- URLを知っている人は書き込めるため、より厳密にしたい場合は
  合言葉（アプリ側でチェック）や認証の追加もできます。私に相談してください。

---

## 共有される範囲（初期実装）
- ✅ **発注履歴**（誰が・どの店舗・何を・いくつ・いくら）→ 全端末で共有
- ❌ 商品マスタ・カートは今まで通り端末ごと（必要なら後で共有化できます）
