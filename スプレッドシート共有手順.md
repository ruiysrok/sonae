# スプレッドシート共有 設定手順（1回だけ・約10分）

これをやると、**スタッフはURL（https://ruiysrok.github.io/sonae/）を開くだけ**で
「誰が・どの店舗で・何を・いくつ」発注したかが全端末で共有されます。
さらに発注履歴が**Googleスプレッドシートに自動記録**されるので、
運営はシートを見るだけで全店の発注を把握できます。

必要なもの：Googleアカウントだけ（無料・カード不要）

---

## ステップ1：スプレッドシートを作る
1. https://sheets.new を開く（Googleスプレッドシートの新規作成）
2. 左上のタイトルを「**そなえ 発注履歴**」などに変更

## ステップ2：Apps Script を開いてコードを貼る
1. シートのメニュー **「拡張機能」→「Apps Script」** をクリック
2. 開いたエディタに最初から入っているコード（`function myFunction() {...}`）を**全部消す**
3. このフォルダの **`gas-code.gs`** の中身を**全部コピーして貼り付け**
4. 上の💾（保存）を押す

## ステップ3：ウェブアプリとして公開する
1. 右上の **「デプロイ」→「新しいデプロイ」**
2. 左の⚙（種類の選択）→ **「ウェブアプリ」**
3. 設定：
   - 説明：`sonae`（何でもOK）
   - 次のユーザーとして実行：**自分**
   - アクセスできるユーザー：**全員** ← ⭐ここ重要（「全員」にしないとアプリから届きません）
4. **「デプロイ」** を押す
5. 「アクセスを承認」→ 自分のGoogleアカウントを選択
   - 「このアプリはGoogleで確認されていません」と出たら
     **「詳細」→「（プロジェクト名）に移動」→「許可」**（自分で作ったアプリなので安全です）
6. 表示される **ウェブアプリのURL**（`https://script.google.com/macros/s/…/exec`）を**コピー**

## ステップ4：URLを私（Claude）に渡す
コピーしたURLを**チャットに貼ってください**。
- このURLは秘密情報ではありません（貼ってOK）。
- 受け取ったら私がアプリに組み込んでデプロイします。
- それ以降、全端末で発注履歴が共有され、シートにも自動記録されます。

---

## 共有される範囲
- ✅ **発注履歴**（誰が・どの店舗・何を・いくつ・いくら）→ 全端末共有＋シート記録
- ❌ 商品マスタ・カートは今まで通り端末ごと（必要なら後で共有化できます）

## 補足
- 履歴の更新はほぼリアルタイム（アプリが30秒ごとに自動で読み直し）。
- URLを知っている人は誰でも記録を送れる仕組みです。社内利用の範囲では
  実用上問題ありませんが、より厳密にしたい場合は合言葉チェックを足せます。
- コードを更新した時は「デプロイ」→「デプロイを管理」→ ✏ →「新しいバージョン」で反映。
