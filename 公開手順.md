# そなえ｜普通のサイトとして公開する手順

このフォルダの **`index.html`** を、どこかのホスティングに置けば公開できます。
中身は1枚の自己完結HTML（外部通信なし・秘密情報なし）なので、そのまま置くだけ。

---

## いちばん簡単（おすすめ）：Netlify Drop
アカウント不要で、ドラッグ&ドロップだけで公開URLが出ます（スマホでも開けます）。

1. ブラウザで **https://app.netlify.com/drop** を開く
2. この **`在庫管理-site`** フォルダ（または `index.html`）を画面にドラッグ&ドロップ
3. 数秒で `https://〇〇〇.netlify.app` のURLが発行される → 完成！

※ URLを残し続けたい・独自ドメインにしたい場合は無料アカウント登録を。

---

## 代替案

### GitHub Pages（無料・URLが安定）
1. GitHubで新規リポジトリを作成
2. `index.html` をアップロード
3. Settings → Pages → Branch を `main` / `/root` に設定
4. `https://ユーザー名.github.io/リポジトリ名/` で公開

### Cloudflare Pages / Vercel（無料・高速）
- アカウント作成 → このフォルダを連携（またはアップロード）するだけ

---

## 注意点（今の仕様）
- データ（発注・履歴・ASIN等）は **開いた端末のブラウザ内** に保存されます。
  公開URLを複数人・複数端末で開いても、**データは共有されません**（各自バラバラ）。
- 「みんなで同じ在庫を見たい」場合は、別途サーバー/データベースが必要（次の段階）。
- 公開URLは知っていれば誰でも開けます。社内限定にしたい場合はパスワード制限のある
  ホスティング（Netlify/Cloudflareの有料機能など）を使ってください。

## 更新のしかた
アプリを直したら、新しい `index.html` を同じ場所に上げ直すだけで反映されます。
